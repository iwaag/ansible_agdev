---
- name: Put macOS hosts to sleep now
  hosts: mac_llm:mac_infra
  gather_facts: false
  become: false

  tasks:
    - name: Put the host to sleep immediately
      ansible.builtin.command: sudo -n /usr/bin/pmset sleepnow
      changed_when: true

