---
- name: Apply Linux worker baseline settings
  hosts: "linux:&power_managed"
  become: true
  gather_facts: true

  roles:
    - role: common_linux_baseline
    - role: linux_worker_tuning
    - role: laptop_power_profile
      when: is_laptop | default(false) | bool
