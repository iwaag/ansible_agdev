---
- name: Suspend Linux hosts now
  hosts: "linux:&power_managed"
  gather_facts: false
  become: false

  tasks:
    - name: Suspend the host immediately
      ansible.builtin.command: sudo -n /usr/bin/systemctl suspend
      changed_when: true
