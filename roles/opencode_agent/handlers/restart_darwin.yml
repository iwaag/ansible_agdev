---
- name: Restart the OpenCode LaunchAgent
  ansible.builtin.command:
    argv:
      - launchctl
      - kickstart
      - -k
      - "gui/{{ ansible_facts['user_uid'] }}/com.clusterintent.opencode.agent"
